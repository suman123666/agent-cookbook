# Python 基础速记

Python 是一门动态类型、解释执行的高级语言，以可读性著称。

## 变量与类型
变量无需声明类型，直接赋值即可。常见内置类型有 int、float、str、bool、list、dict、tuple、set。

## 函数
用 def 定义函数，支持默认参数、关键字参数、可变参数（*args / **kwargs）。函数是一等公民，可以当参数传递。

## 虚拟环境
推荐用虚拟环境隔离每个项目的依赖，避免版本冲突。现代工具如 uv 能同时管理 Python 版本和依赖，速度远快于传统 pip + venv。
