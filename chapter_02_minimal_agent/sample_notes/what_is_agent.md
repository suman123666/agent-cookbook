# 什么是 AI Agent

Agent 是能自主决策、调用工具、循环执行直到完成任务的 LLM 应用。

## 与 Workflow 的区别
Workflow 的执行路径由程序员预先写死；Agent 的路径由 LLM 在运行时自己决定。

## 三大支柱
Agent 离不开三样东西：LLM 决策核心（大脑）、工具集（手脚）、反馈循环（心跳）。缺任何一个都不算真正的 agent。

## 何时该用
只有当任务步骤无法预测、需要根据中间结果动态决策、且能容忍偶尔失败时，才值得上 agent。否则用 workflow 更稳更省。
